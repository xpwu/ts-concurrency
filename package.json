{
  "name": "ts-concurrency",
  "version": "0.3.0",
  "description": "typescript concurrency: channel, mutex, semaphore and so on",
  "author": "xpwu",
	"type": "module",
	"main": "./dist",
	"types": "./dist",
	"dependencies": {
		"ts-x": "github:xpwu/ts-x#semver:^0.5.2"
	},
  "devDependencies": {
		"@types/jest": "30.0.0",
		"jest": "30.1.3",
		"rimraf": "^6.0.1",
		"ts-jest": "29.4.4"
  },
  "scripts": {
		"clean": "rimraf ./dist",
		"tsc": "npm run clean && tsc",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "keywords": [
    "concurrency",
    "channel",
		"mutex",
		"semaphore"
  ],
  "license": "MIT",
  "files": [
    "src",
		"dist",
    "index.ts"
  ],
	"engines": {
		"node": ">=24.8.0"
	},
  "repository": {
    "type": "git",
    "url": "https://github.com/xpwu/ts-concurrency.git"
  },

  "miniprogram": "."
}
